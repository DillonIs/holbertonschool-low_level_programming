#include "main.h"

/**
 *
 *
 */

void jack_bauer(void)
{
int m = 0;
int h = 0;
while (h < 23)
  {
    h++;
  }
while (m < 59)
  {
m++;
  }
_putchar('0' + h / 10);
_putchar('0' + h % 10);
_putchar(':');
_putchar('0' + m / 10);
_putchar('0' + m % 10);
_putchar('\n');
}
