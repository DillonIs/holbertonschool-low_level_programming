#include "main.h"

/**
 * _abs - finds the absolute value
 *
 * @n: n is the value being tested
 *
 * Return: 0 if false
 *
 */

int _abs(int n)
{
  if (n <= 48)
  return (n * -1);
  else
  return (n);
}
