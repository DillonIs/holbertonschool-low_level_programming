#include <studio.h>

/**
 * main - Entry point always starts the code
 *
 * Return always 0 when successful
 */
int main(void)
{
  puts("Programming is like building a multilingual puzzle");
  return (0);
}
