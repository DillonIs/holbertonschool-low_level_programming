#include <stdio.h>

/**
 * main - Entry point of program
 * @argc: Number of commands issued
 * @argv: NULL for this program
 *
 * Return: Returns back to 0.
 */

int main(int argc, char *argv[])
{
printf("%d\n", argc - 1);
(void)argv;
return (0);
}
