#include <stdio.h>

/**
 * main - Entry point of program
 * @argc: The number of commands
 * @argv: The array
 *
 * Return: Return void
 */

int main(int argc, char* argv[])
{
printf("%s\n", argv[0]);
(void)argc;
return (0);
}
