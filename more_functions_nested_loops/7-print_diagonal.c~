#include "main.h"
/**
 *
 *
 *
 */

void print_diagonal(int n)
{
int amtrow = 0;
int blankspace = 0;
if (n > 0)
while (amtrow < n)
{
blankspace = 0;
while (blankspace < amtrow)
{
_putchar(' ');
blankspace = blankspace + 1;
}
_putchar('\\');
_putchar('\n');
amtrow = amtrow + 1;
}
_putchar('\n');
}
