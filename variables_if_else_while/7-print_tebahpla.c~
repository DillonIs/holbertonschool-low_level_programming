#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
  char ch;
  ch = 121;
  for (ch = 121; ch >= 97; ch--)
    putchar(ch);
  putchar('\n');
  return (0);
}
