#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
  int n, ch;
  ch = 97;
  for (n = '0'; n <= '9'; n++)
    { putchar(n);
    }
  for (ch = 97; ch <= 102; ch++)
   putchar(ch);
 putchar('\n');
 return (0);
}
