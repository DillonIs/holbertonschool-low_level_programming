#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

/**
 *
 *
 *
 *
 */

void *malloc_checked(unsigned int b)
{
  void *point;

  point = malloc(b);
  if (point == NULL)
    {
      exit(98);
    }
  return (point);
}
