#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 *
 *
 *
 */

list_t *add_node_end(list_t **head, const char *str)
{
	list_t *list, *last_node;

	last_node = malloc(sizeof(list_t));

	
}

